# 灵笔 (Lingbi) 一键启动器
name: lingbi_launcher
description: 灵笔一键启动器
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.4.0
  system_tray: ^2.0.3
  path: ^1.9.0
  process_run: ^1.1.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon.ico